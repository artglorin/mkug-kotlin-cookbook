plugins {
    id 'kotlin-multiplatform' version '1.3.21'
}
repositories {
    mavenCentral()
}
kotlin {
    macosX64("sample") {
        compilations.main {
            outputKinds("EXECUTABLE")
            entryPoint 'sample.main'
            cinterops {
                curl { // Имя файла .def
                    
                }
            }
        }
    }
    sourceSets {
        sampleMain {
        }
    }
}

task runProgram {
    def buildType = 'RELEASE'
    dependsOn "linkMainReleaseExecutableSample"
    doLast {
        def targ = kotlin.targets
        println(kotlin.targets.sample)
        def programFile = kotlin.targets.sample.compilations.main.getBinary('EXECUTABLE', buildType)
        exec {
            executable programFile
            args 'MKUG', "This is {{name}}!", 49
        }
    }
}